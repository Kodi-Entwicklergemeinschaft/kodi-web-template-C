import { Box } from "@mui/material";
import FavoritesContent from "../FavoritesContent";

const FavoritesWithFilter = () => {
    return (
        <Box>
            <FavoritesContent />
        </Box>
    );
};

export default FavoritesWithFilter;
